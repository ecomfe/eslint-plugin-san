san.defineComponent({
    template: `{{ . }}`,
})

// @san/component
export class A {
    static template = `
        <div class="{{*abc*}}" / on-click="def(">
            </span>
        </div id="ghi">
    `;
}

export default {
    template: `{{ foo bar }}`,
}
